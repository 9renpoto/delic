box: wercker/nodejs
build:
  steps:
    - npm-install
    - script:
        name: lint
        code: $(npm bin)/eslint src
  after-steps:
    - slack-notifier:
      url: $SLACK_URL
      channel: $SLACK_CHANNEL
      username: $SLACK_WERCKER_BOT_NAME
